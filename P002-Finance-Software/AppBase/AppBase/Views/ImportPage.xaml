<Page
    x:Class="AppBase.Views.ImportPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AppBase.Views"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Padding="20" Spacing="10">
        <Button Content="Ouvrir un fichier Excel" Click="OnOpenFileClick"/>
        <Button Content="Enregistrer" Click="OnSaveJsonClick"/>
        
        <controls:DataGrid x:Name="DataGridExcel" AutoGenerateColumns="False" ItemsSource="{x:Bind Donnees, Mode=OneWay}">
            <controls:DataGrid.Columns>
                <controls:DataGridTextColumn Header="Date" Binding="{Binding Date}" />
                <controls:DataGridTextColumn Header="Prix" Binding="{Binding Prix}" />
                <controls:DataGridTextColumn Header="Description" Binding="{Binding Description}" />
                <controls:DataGridTextColumn Header="Personne" Binding="{Binding Personne}" />
                <controls:DataGridComboBoxColumn Header="Categorie"
                                  Binding="{Binding Categorie, Mode=TwoWay}"
                                  ItemsSource="{x:Bind Categories}" />

            </controls:DataGrid.Columns>
        </controls:DataGrid>

    </StackPanel>

</Page>
